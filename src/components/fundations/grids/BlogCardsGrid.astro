---
const {
  class: additionalClass = "", // Allow passing extra class names
  tag = "ul", // Allow switching between ul/ol/div if needed
} = Astro.props;
const Tag = tag;
---

<Tag
  class={`grid grid-cols-1 md:grid-cols-3 gap-8 gap-y-24 mt-4 ${additionalClass}`}
>
  <slot />
</Tag>
